export default class EasterEgg {
    constructor(public name: string, public version: string, public time: number) {
        this.name = name;
        this.version = version;
        this.time = time;
    }
}